A pasta everionments deve ser excluida para o projeto que esta sendo configurada, essa sessão é utilizada apenas para buildar ajustes na imagem base e deverá ser utilizada apenas dentro do repositório

`tim_setup_docker`